<div class="page-header">
  <%= link_to new_persona_establecimiento_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Nuevo Persona establecimiento
  <% end %>
  <h1>Listado de persona_establecimientos</h1>
</div>

<div class="table-responsive">
  <table id="tabla-persona_establecimiento" class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Persona</th>
            <th>Establecimiento</th>
            <th class="text-center">Acciones</th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @persona_establecimientos) do |persona_establecimiento| %>
            <td><%= persona_establecimiento.persona_id %></td>
            <td><%= persona_establecimiento.establecimiento_id %></td>
            <td class="text-center">
          <div class="btn-group">
            <a class="btn dropdown-toggle btn-primary" data-toggle="dropdown" href="#">
              Acciones
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Ver', persona_establecimiento %></li>
              <li><%= link_to 'Editar', edit_persona_establecimiento_path(persona_establecimiento) %></li>
              <li><%= link_to 'Eliminar', persona_establecimiento, method: :delete, data: { confirm: '¿Seguro desea eliminar?' } %></li>
              </li>
            </ul>
          </div>
        </td>
      <% end %>
    </tbody>
  </table>
</div>

<script type="text/javascript">
    $(document).ready(function($) {
      var cantidad = $("table > tbody").find("> tr:first > td").length - 1;
      var pepe = $('#tabla-persona_establecimiento').dataTable({
        "sPaginationType": "bootstrap",
        "aoColumnDefs": [
          { "bSearchable": false, "aTargets": [ cantidad ] }
        ],
        "oLanguage": {
              "sUrl": "language.es.json",
          },
        "aaSorting": [[ 0, "desc" ]],
      });
    });
</script>