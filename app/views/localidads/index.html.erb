<div class="page-header">
  <%= link_to new_localidad_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Nuevo Localidad
  <% end %>
  <h1>Listado de localidades</h1>
</div>

<div class="table-responsive">
  <table id="tabla-localidad" class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Nombre</th>
            <th>Region</th>
            <th>Cp</th>
            <th class="text-center">Acciones</th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @localidads) do |localidad| %>
            <td><%= localidad.nombre %></td>
            <td><%= localidad.region %></td>
            <td><%= localidad.cp %></td>
            <td class="text-center">
          <div class="btn-group">
            <a class="btn dropdown-toggle btn-primary" data-toggle="dropdown" href="#">
              Acciones
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Ver', localidad %></li>
              <li><%= link_to 'Editar', edit_localidad_path(localidad) %></li>
              <li><%= link_to 'Eliminar', localidad, method: :delete, data: { confirm: '¿Seguro desea eliminar?' } %></li>
              </li>
            </ul>
          </div>
        </td>
      <% end %>
    </tbody>
  </table>
</div>

<script type="text/javascript">
    $(document).ready(function($) {
      var cantidad = $("table > tbody").find("> tr:first > td").length - 1;
      var pepe = $('#tabla-localidad').dataTable({
        "sPaginationType": "bootstrap",
        "aoColumnDefs": [
          { "bSearchable": false, "aTargets": [ cantidad ] }
        ],
        "oLanguage": {
              "sUrl": "language.es.json",
          },
        "aaSorting": [[ 0, "desc" ]],
      });
    });
</script>