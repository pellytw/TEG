<div class="page-header">
  <%= link_to new_empresa_transporte_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Nuevo Empresa transporte
  <% end %>
  <h1>Listado de empresa_transportes</h1>
</div>

<div class="table-responsive">
  <table id="tabla-empresa_transporte" class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Nombre</th>
            <th>Localidad</th>
            <th class="text-center">Acciones</th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @empresa_transportes) do |empresa_transporte| %>
            <td><%= empresa_transporte.nombre %></td>
            <td><%= empresa_transporte.localidad_id %></td>
            <td class="text-center">
          <div class="btn-group">
            <a class="btn dropdown-toggle btn-primary" data-toggle="dropdown" href="#">
              Acciones
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Ver', empresa_transporte %></li>
              <li><%= link_to 'Editar', edit_empresa_transporte_path(empresa_transporte) %></li>
              <li><%= link_to 'Eliminar', empresa_transporte, method: :delete, data: { confirm: '¿Seguro desea eliminar?' } %></li>
              </li>
            </ul>
          </div>
        </td>
      <% end %>
    </tbody>
  </table>
</div>

<script type="text/javascript">
    $(document).ready(function($) {
      var cantidad = $("table > tbody").find("> tr:first > td").length - 1;
      var pepe = $('#tabla-empresa_transporte').dataTable({
        "sPaginationType": "bootstrap",
        "aoColumnDefs": [
          { "bSearchable": false, "aTargets": [ cantidad ] }
        ],
        "oLanguage": {
              "sUrl": "language.es.json",
          },
        "aaSorting": [[ 0, "desc" ]],
      });
    });
</script>